import { StyleSheet, Text, View, Press, TouchableOpacity, } from 'react-native'
import React from 'react'


const Form = ({navigation}) => {

  return (
 <View>
 <TouchableOpacity>
  <Text style={{fontSize:29,color:'red'}}>
    sohil
  </Text>
 </TouchableOpacity>

 </View>
  )
}

export default Form

const styles = StyleSheet.create({})