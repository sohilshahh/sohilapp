import { StyleSheet, Text, View,TouchableOpacity } from 'react-native'
import React from 'react'

const Button = ({text='Btn', onpress=()=>{},style}) => {
  return (
    <View>
  
    </View>
  )
}

export default Button

const styles = StyleSheet.create({})